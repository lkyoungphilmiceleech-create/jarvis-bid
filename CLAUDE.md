# 마이스리치 해외마켓본부 — 업무 자동화 규정

## 1. 조직

| 부서 | 직원 | 담당 산출물 |
|---|---|---|
| 투자리서치 (`inv-`) | inv-scout, inv-classify, inv-cases, inv-people, inv-contact, inv-report | 투자사 조사 리포트 |

부서장(오케스트레이터)은 메인 세션이며, 파이프라인 순서는 스킬이 강제한다.
- 투자사 조사 릴레이: `/investor-relay`

## 2. 릴레이 원칙 (전 부서 공통)

1. **배턴은 대화가 아니라 파일이다.** 각 단계는 직전 단계의 JSON만 입력으로 받고, 자기 단계 JSON을 쓴다.
2. **직원은 옆 직원을 부르지 않는다.** 호출은 오케스트레이터만 한다.
3. **게이트를 통과해야 다음 단계로 간다.** 미달이면 멈추고 보고한다.
4. **삭제 대신 격리한다.** 제외는 `dropped`, 불확실은 `needs_review`로 남긴다.
5. **중단되면 마지막 파일 번호부터 재개한다.** 처음부터 다시 돌리지 않는다.

## 3. 전사 규칙

- 보고는 결론 먼저(PREP), 개조식(~함/~임).
- 모든 수치·기관명·인명에 출처를 병기한다. 출처 없으면 '확인 필요'로 분리한다.
- 근거 없는 추정치를 만들지 않는다. 모르면 `null` 또는 "비공개".
- 개인 연락처는 **공개 게시된 업무용 정보만** 수집한다. 이메일 패턴 추측 생성 금지.
- **대외 발송물(메일·DM·제안서)은 초안까지만 자동화한다. 발송은 반드시 마스터 승인 후.**

## 4. 데이터 위치

| 용도 | 경로 |
|---|---|
| 입찰공고 수집 결과 | `data/latest.json`, `data/YYYY-MM-DD.json` |
| 투자사 조사 릴레이 | `data/investors/<run_id>/00_brief.json ~ 05_contacts.json` |
| 배턴 스키마 | `.claude/schemas/investor-relay.md` |
